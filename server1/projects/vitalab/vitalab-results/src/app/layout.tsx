import { Header } from "~/components/header";
import "./globals.css";

import { Inter } from "next/font/google";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="uk" className={`${inter.variable}`}>
      <body>
        <main className="max-w-4xl flex flex-col relative min-h-screen px-6 py-32 mx-auto">
          <div className="flex items-stretch justify-start flex-nowrap flex-col">
            <Header />
            <div className="border border-gray-200 rounded-xl flex relative select-none bg-gray-100 min-h-[300px]">
              {children}
            </div>
          </div>
        </main>
      </body>
    </html>
  );
}
