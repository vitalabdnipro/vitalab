const TubeColor = ({ color }) => (
  <div
    className="h-5 w-14 inline-flex items-center px-2 py-0.5 rounded border border-gray-200"
    style={{ backgroundColor: `${color}` }}
  />
);

export default TubeColor;
